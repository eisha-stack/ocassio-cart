import type { Product } from '@/types/product';

// TODO: render a single product/bundle item once the bundle data shape is finalized.
export function BundleCard({ product: _product }: { product: Product }) {
  return <div>{/* TODO: bundle card placeholder */}</div>;
}
