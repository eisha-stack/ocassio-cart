import type { Cart } from '@/types/cart';

// TODO: render cart contents and totals, plus the "confirm order" call-to-action.
export function CartSummary({ cart: _cart }: { cart: Cart }) {
  return <div>{/* TODO: cart summary placeholder */}</div>;
}
