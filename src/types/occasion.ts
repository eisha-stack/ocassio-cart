/**
 * TODO: define the full Occasion domain model once business requirements are finalized
 * (e.g. birthday, house party, Diwali, game night).
 */
export interface Occasion {
  id: string;
  name: string;
  description?: string;
}
